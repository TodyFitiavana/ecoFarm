import React, { FC } from 'react';

const FarmerDashboardheader : FC = () : JSX.Element => {
    return (
        <header className='farmer-header w-full overflow-hidden h-[65px] bg-white'>
            
        </header>
    );
};

export default FarmerDashboardheader;